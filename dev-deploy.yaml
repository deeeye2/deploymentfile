apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
  namespace: development
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: web-container
          image: httpd:2.4
          ports:
            - containerPort: 80
